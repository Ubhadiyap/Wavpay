<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:custom="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/white"
    android:descendantFocusability="blocksDescendants"
    android:orientation="vertical">

    <ImageView
        android:id="@+id/item_apply_loans_photo_iv"
        android:layout_width="60dp"
        android:layout_height="60dp"
        android:layout_centerInParent="true"
        android:background="@mipmap/ic_photo_add_bg"
        android:scaleType="centerCrop" />

    <ImageButton
        android:id="@+id/item_apply_loans_photo_delete_ib"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignRight="@id/item_apply_loans_photo_iv"
        android:layout_alignTop="@id/item_apply_loans_photo_iv"
        android:background="@null"
        android:src="@mipmap/ic_image_delete"
        android:visibility="gone" />

</RelativeLayout>